import type { TPluginsConfig } from './src/core/plugins/types';

export const plugins: TPluginsConfig = {
	tma: {
		enabled: true,
	},
	pwa: {
		enabled: true,
	},
	examples: {
		enabled: true,
	},
};
